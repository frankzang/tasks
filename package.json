{
  "name": "tasks",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild --bundle --platform=node src/index.ts",
    "build:production": "npm run build  -- --outdir=dist",
    "serve": "nodemon src/index.ts",
    "migrate": "npx prisma migrate dev --name init"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/frankzang/tasks.git"
  },
  "author": "frankzang",
  "license": "UNLICENSED",
  "private": true,
  "bugs": {
    "url": "https://github.com/frankzang/tasks/issues"
  },
  "homepage": "https://github.com/frankzang/tasks#readme",
  "dependencies": {
    "@prisma/client": "^3.4.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "dotenv": "^10.0.0",
    "esbuild": "^0.13.12",
    "nodemon": "^2.0.14",
    "prisma": "^3.4.0",
    "ts-node": "^10.4.0",
    "typescript": "^4.4.4"
  }
}
